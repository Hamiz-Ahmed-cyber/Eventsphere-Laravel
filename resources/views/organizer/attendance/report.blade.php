@extends('layouts.organizer')
@section('title', 'Attendance Report')
@section('content')<div class="card overflow-x-auto"><div class="flex justify-between items-center mb-5"><div><h2 class="font-display text-lg font-semibold">{{ $event->title }}</h2><p class="text-sm text-ink-300">{{ $attended->count() }} attendee(s) checked in</p></div><a href="{{ route('organizer.events.show', $event) }}" class="btn-outline">Back to Event</a></div><table class="w-full text-sm"><thead><tr class="text-left text-ink-300 border-b border-base-700"><th class="pb-3">Participant</th><th class="pb-3">Department</th><th class="pb-3">Attendance</th></tr></thead><tbody>@forelse($event->registrations->where('status','confirmed') as $registration)<tr class="border-b border-base-700/50"><td class="py-3 text-ink-50">{{ $registration->student->name }}</td><td class="py-3 text-ink-300">{{ $registration->student->details->department ?? '—' }}</td><td class="py-3">@if($attended->contains($registration->student_id))<span class="badge-approved">Attended</span>@else<span class="badge-pending">Not attended</span>@endif</td></tr>@empty<tr><td colspan="3" class="py-6 text-ink-300">No confirmed registrations.</td></tr>@endforelse</tbody></table></div>@endsection
