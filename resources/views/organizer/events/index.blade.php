@extends('layouts.organizer')
@section('title', 'My Events')
@section('content')<div class="flex justify-end mb-5"><a href="{{ route('organizer.events.create') }}" class="btn-primary">＋ Create Event</a></div><div class="card overflow-x-auto"><table class="w-full text-sm"><thead><tr class="text-left text-ink-300 border-b border-base-700"><th class="pb-3">Event</th><th class="pb-3">Date</th><th class="pb-3">Status</th><th class="pb-3">Registrations</th><th class="pb-3">Actions</th></tr></thead><tbody>@forelse($events as $event)<tr class="border-b border-base-700/50"><td class="py-3 text-ink-50">{{ $event->title }}</td><td class="py-3 text-ink-300">{{ $event->event_date->format('d M Y') }}</td><td class="py-3"><span class="badge-{{ $event->status === 'approved' ? 'approved' : ($event->status === 'rejected' ? 'rejected' : 'pending') }}">{{ ucfirst($event->status) }}</span></td><td class="py-3 text-ink-300">{{ $event->registrations_count }}</td><td class="py-3 flex gap-2"><a href="{{ route('organizer.events.show', $event) }}" class="btn-outline !py-1">View</a>@if($event->event_date->isFuture() && $event->status !== 'cancelled')<a href="{{ route('organizer.events.edit', $event) }}" class="btn-outline !py-1">Edit</a>@endif</td></tr>@empty<tr><td colspan="5" class="py-8 text-ink-300">No events yet.</td></tr>@endforelse</tbody></table></div><div class="mt-5">{{ $events->links() }}</div>@endsection
